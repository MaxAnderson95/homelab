apiVersion: v1
kind: Service
metadata:
  name: minecraft-server-survival
  annotations:
    mc-router.itzg.me/externalServerName: "survival.mc.maxanderson.tech"
spec:
  type: ClusterIP
  selector:
    app: minecraft-server-survival
  ports:
    - port: 25565
      targetPort: 25565
      protocol: TCP
      name: minecraft
