apiVersion: apps/v1
kind: Deployment
metadata:
  name: minecraft-server-legacy1
  labels:
    app: minecraft-server-legacy1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minecraft-server-legacy1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: minecraft-server-legacy1
    spec:
      volumes:
        - name: minecraft-legacy1-data
          persistentVolumeClaim:
            claimName: minecraft-legacy1-data-pvc
      containers:
        - name: minecraft-server-legacy1
          image: itzg/minecraft-server:latest
          resources:
            limits:
              memory: "2Gi"
            requests:
              memory: "1.5Gi"
          envFrom:
            - configMapRef:
                name: minecraft-legacy1-env-configmap
          env:
            - name: OPS
              valueFrom:
                configMapKeyRef:
                  name: minecraft-global-config-ops-list
                  key: ops.txt
            - name: PLUGINS
              valueFrom:
                configMapKeyRef:
                  name: minecraft-legacy1-plugins-list-configmap
                  key: plugins.txt
          ports:
            - containerPort: 25565
              name: minecraft
          volumeMounts:
            - name: minecraft-legacy1-data
              mountPath: /data
          readinessProbe:
            exec:
              command:
                - mcstatus
                - 127.0.0.1
                - ping
            initialDelaySeconds: 5
            periodSeconds: 30
          livenessProbe:
            exec:
              command:
                - mcstatus
                - 127.0.0.1
                - ping
            initialDelaySeconds: 30
            periodSeconds: 30
